读取subscription_checkout_cases.csv、quote_responses.json和test_contract.json，建立场景、报价响应、浏览器时间和视口之间的对应关系。
修复playwright.config.mjs，让Chromium从本机静态服务器访问结账页，并把测试输出隔离在当前任务目录。
按deep_link打开每条场景，使用role、label或test id定位套餐、地区、优惠码、支付方式、状态提示、总价和继续按钮。
拦截test_contract.json规定的报价请求，按场景fixture依次返回状态与正文，并核对最终请求参数。
固定浏览器时间，核对场景结果、按钮状态和可聚焦控件，按场景保存trace与完整页面截图。
生成场景结果、网络合同、证据清单和运行摘要，按case_id复核报表与实际证据文件的对应关系。
通过test:e2e入口从干净目录运行，确认成功时产物完整，失败时本轮reports与evidence被清理。
